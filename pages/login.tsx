const Login = () => {
  return (
    <div>
      <h1>로그인</h1>
      <input defaultValue="id" />
      <input defaultValue="pw" />
    </div>
  );
};

export default Login;

// title
// message
// hashtag
// kakao link (둘 중 하나)
// discord link (둘 중 하나)
